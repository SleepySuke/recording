# T14：交付验收与文档回写

> 依赖：T13 · 预算：0.5h · 状态：**未开始**

**设计依据**：需求原文提交内容四项（README / 一键启动 / API 调试文件 / 完整 commit 历史）；测试设计附录 A（真实 LLM 人工验收）；HANDOFF §4（代码实现后必须回写 README）
**测试依据**：验收清单「交付」全条目；附录 A 三条

## 目标

交付四件材料齐备且据实：`api/recordings.http`、README 回写（功能状态与运行验证结果）、真实 LLM 人工验收记录、仓库卫生与 commit 历史检查。

## 涉及文件

- Create：`api/recordings.http`（覆盖六个接口）
- Modify：`README.md`（据实回写）
- Modify：`HANDOFF.md`（更新当前状态与剩余缺口，或标注完成）
- Modify：本文件（记录验收结果）

## 步骤

- [ ] 1. 编写 `api/recordings.http`：六个接口 + 变量化的 {{base_url}}/{{recording_id}}/{{task_id}} 占位；附注释说明「查看失败任务并重试」的操作路径（GET 任务 → 确认 failed → retry）。
- [ ] 2. 真实 LLM 人工验收（测试设计附录 A，逐条）：
  - [ ] 真实渠道上传 → done，三个摘要字段来自真实模型（脱敏样例记入 README）。
  - [ ] 断网 / 改错 Key → failed/50002，响应不含 Key 与堆栈。
  - [ ] 核对调用次数与费用符合预期（重启恢复场景注意重复调用）。
- [ ] 3. README 回写：功能状态表改为实际状态；运行命令与验证结果据实更新（去掉"设计阶段/尚未实现"措辞）；补已知问题与未完成项（如重启可能重复调用 LLM、单实例约束、无上传幂等）；确认架构图/ER/状态机与代码一致，不一致处先改代码或补图。
- [ ] 4. 演示走查（原计划演示顺序）：启动展示迁移 → 上传 → 查询阶段 → 成功摘要 → 失败与手动重试 → 分页列表 → 删除与清理确认 → 在途重启恢复 → 核心测试运行。
- [ ] 5. 仓库卫生：无 API Key / 真实录音 / 数据库文件 / 构建产物入库；`git log` 呈现逐步开发（≥ 14 个功能提交，无一次性大提交）。
- [ ] 6. 提交：`docs: finalize delivery docs and acceptance records`。

## 完成标准

- [ ] 新环境按 README 两步启动成功；调试文件可直接导入使用。
- [ ] README 与实际行为一致（含真实 LLM 脱敏样例）；需求四项提交材料全部就位。
